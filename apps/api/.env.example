NODE_ENV=development
PORT=3000
HOST=127.0.0.1
# guest 仅供本地开发；正式小程序使用 wechat。
AUTH_MODE=guest
# WECHAT_APP_ID=
# WECHAT_APP_SECRET=
# 未设置 DATABASE_URL 时使用项目 .data/pglite 持久化数据库。
# DATABASE_URL=postgresql://USER:PASSWORD@127.0.0.1:5432/smoke_mock
# 生产默认不自动迁移，首次初始化时显式设置为 true。
AUTO_MIGRATE=false

