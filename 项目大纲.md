# 第一人称抽烟模拟器项目大纲

- 版本：v0.2（主要功能实现后更新）
- 整理日期：2026-09-18
- 项目位置：D:\MyAgent\Empower\smoke_mock
- 产品名暂定「此刻」，工程名 smoke-mock；正式名称待确定。
- 本文区分已经确认的产品要求、工程默认值和后续工作，不把基础骨架等同于成品。

## 一、产品定位

面向成年用户的第一人称虚拟抽烟互动体验。画面可见双手，用户可以选择眼前的场景，完成点燃、模拟吸入、吐出烟雾、抖灰、熄灭等操作。通过场景、当前时间和预设事件形成每一轮的情境。

参考产品只用于理解同类功能。整体界面、手部素材、文案、图标、音效和代码独立制作或合法授权。2026-09-21 用户确认商店加入四款真实品牌参考包装；当前素材用于开发验证，来源和待确认授权状态见 docs/商品素材说明.md。

## 二、已确认的产品原则

1. 第一人称视角，能看见两只手操作，眼前场景可切换。
2. 商品页采用货架橱窗，展示虚拟烟款与虚拟币售价，不用真实货币购买。
3. 情境由「时间 + 地点 + 事件」组成。
4. 时间直接使用当前时间，不提供用户填写或选择入口。
5. 地点使用用户选择的地点，不申请定位，不推断真实位置。
6. 事件由运营者预设，按时间段与地点匹配后随机触发。
7. 用户不选择、不填写、不确认事件，也不等待事件文字播放完毕才能操作。
8. 事件只在一轮开始时生成一次；切后台、回到页面不重新抽取。
9. 以简洁操作和沉浸感为优先，不采用参考产品那种密集侧边按钮排列。

## 三、首版范围

| 模块 | 首版目标 | 当前骨架状态 |
| --- | --- | --- |
| 此刻页面 | 地点选择、当前时间、随机情境、第一人称操作 | 页面与交互状态已搭建 |
| 双手 | 左手点火、右手持烟、抬手/回落/弹灰/按灭 | Canvas 插画式双手与交互动画已实现，真机待验收 |
| 场景 | 公司厕所、出租屋阳台；支持日间/黄昏/夜间气氛 | 原创程序化场景、时段和烟雾已实现 |
| 货架橱窗 | 原创外观与品牌参考包装、虚拟币价格、兑换、数量展示 | 三款原创外观、四款品牌参考及兑换流程 |
| 收藏 | 库存、选用下一轮外观、操作震动 | 基础页面已搭建 |
| 事件库 | 时间与地点条件、权重、冷却、通用兜底 | 共享领域规则已实现 |
| 后端 | 登录、初始化、兑换、进度、开始/结束体验、补给、偏好与记录 | NestJS 持久化业务 API 已实现 |
| 持久化 | 玩家状态、身份令牌与钱包流水 | PGlite 落盘已测试；独立 PostgreSQL 适配器已实现 |

首版不加入多人递烟、排行榜、VIP、充值、提现、真实商品购买、麦克风识别和自由三维视角。更丰富的情境相册、更多外观和场景属于后续候选功能；当前已提供只读历史情境列表。

## 四、用户旅程

### 4.1 首次进入

打开「此刻」→ 默认提供免费基础款和一个地点 → 用户可换地点 → 点击进入 → 自动显示「当前时间 · 所选地点」与一句事件 → 可以立即操作。

没有事件表单，没有事件选项，没有「确认故事」弹窗。首次默认地点为公司厕所，这是工程默认值，可根据后续反馈调整。

### 4.2 再次进入

记住上次地点和下一轮使用的商品。若存在未结束的体验，恢复该会话和事件；后台期间暂停演示燃烧，回来后不额外扣库存、不重新生成事件。已结束时可以开始新一轮。

每次开始消耗一支已兑换商品，免费基础款不限量。开始前扣减库存属于首版默认规则，即使尚未点燃后就结束，也不退回；如后续改为点燃时扣减，需要同步变更会话协议。

### 4.3 橱窗与收藏

橱窗陈列原创外观与品牌参考包装 → 显示「火花币 / 盒」与每盒支数 → 兑换成功更新余额与库存 → 收藏中选用 → 下一轮使用。

基础款随时可用，不因余额不足阻断核心体验。已开始的一轮保持原商品，收藏切换仅影响下一轮。

## 五、第一人称交互

| 状态/动作 | 预期表现 |
| --- | --- |
| 待点燃 | 右手持烟在下方，左手与打火机位于左下方 |
| 点燃 | 左手靠近烟头，右手轻微调整角度，火焰与烟头衔接 |
| 自然燃烧 | 烟支逐渐缩短，积累烟灰，烟头有弱光 |
| 长按模拟吸入 | 右手向嘴边抬起，烟头增强，燃烧加快 |
| 松开 | 右手回落，烟雾从镜头下方进入视野后扩散 |
| 抖灰 | 右手向场景指定位置移动，灰烬掉落 |
| 熄灭 | 场景适配的结束动作，状态归零 |
| 切后台/触摸取消 | 结束持续按压、停止动画和定时器，保存会话状态 |

固定视角和左右手分工，不做真实手势识别。公司厕所、阳台分别定义道具位置，不强行共享同一张桌面。动画系统后续用渲染适配层承接，业务逻辑不依赖具体素材。

当前 Canvas 实现插画式握持、烟灰累计、烟雾、合成音效和动作状态；可继续打磨写实程度，真机效果待验收。

## 六、场景与情境事件

### 6.1 时间来源

- 默认以中国大陆用户为范围，统一使用 Asia/Shanghai（UTC+8）。
- API 模式使用服务端当前时间；本地演示使用设备当前时间。
- 按会话开始时间保存 HH:mm 与时段，恢复时沿用该情境时间。
- 页面未进入会话时自动更新当前时间；进入会话后保留开局的情境时间。
- 未来面向其他时区时需修改策略，不增加用户输入负担。

### 6.2 事件数据

| 字段 | 含义 |
| --- | --- |
| id / text | 稳定标识、展示文案 |
| sceneIds | 适用地点列表 |
| startMinute / endMinute | 时间窗口，左闭右开，可跨午夜 |
| weekdays | 可选，0 周日至 6 周六；不填表示不限 |
| weight | 同一候选池内出现概率权重，正数 |
| cooldownMinutes | 同一玩家再次出现的最小间隔 |
| fallback | 是否为地点通用兜底事件 |
| enabled | 是否启用 |

工作日当前按周一至周五判断，不包含法定节假日调休信息。

### 6.3 选择流程

1. 过滤禁用、地点不符、时段不符、星期不符的事件。
2. 排除冷却中的事件和上一轮刚出现的事件。
3. 如果有符合条件的专属情境事件，按权重抽取。
4. 否则使用仍可用的通用事件。
5. 全部候选都在冷却时，放宽冷却，但有替代候选时仍避免与上一轮相同。
6. 只有一个合法候选时允许重复；绝不为防重复而选取错误地点或时段的事件。
7. 每个启用场景至少维护两条覆盖全天的通用文案。
8. 事件文本随会话保存快照；运营修改事件库不改变已有会话。

示例：「10:16 · 公司厕所 / 点上一支烟，开启粑粑时间。」

文案淡入后自动淡出，不遮挡核心操作，不绑定奖励、不改变商品价格。阶段一通过源码维护事件库，阶段二再做运营管理界面。

## 七、虚拟商品与经济

以下金额、数量和名称为可调整的工程默认值：

| 商品 | 价格 | 数量 |
| --- | --- | --- |
| 留白 | 免费 | 基础款不限量 |
| 雨后 | 20 火花币 | 10 支 / 盒 |
| 夜班 | 30 火花币 | 10 支 / 盒 |

初始 100 火花币，每个北京时间自然日领取 20 火花币。没有充值、提现、交易或抽奖。奖励不取决于模拟吸烟数量。

API 只接受商品 ID，价格与库存由服务端决定。兑换请求必须带幂等编号；同编号同商品返回已有结果，同编号不同商品拒绝。生产实现需在数据库事务中处理余额、库存和账本。

## 八、页面与信息架构

- 此刻：时间、地点选择、第一人称环境、双手、短暂事件文案、点燃/吸入/抖灰/结束。
- 橱窗：虚拟币余额、货架商品、价格、库存、兑换、每日补给。
- 收藏：已持有商品、下一轮外观选择，以及钱包、历史情境和设置入口。
- 后续管理端：场景、商品、事件编辑，启停与权重调整；不在当前搭建范围内。

## 九、技术架构

前端：微信原生小程序 + TypeScript + esbuild。WXML/WXSS 负责界面，Canvas 2D 已负责场景、双手、烟灰和烟雾动画。暂不引入三维引擎。

后端：NestJS 11 + TypeScript，REST API，DTO 参数校验，领域错误标准化。npm workspaces 管理前后端，共享包复用类型、演示种子和业务规则。

持久化：本地使用 PGlite（嵌入式 PostgreSQL）落盘，独立 PostgreSQL 使用 pg 连接池。当前采用玩家 JSONB 聚合、令牌表、独立钱包流水和迁移版本表；事务与行锁保障经济操作。本地自动初始化，生产显式迁移。微信登录适配已实现，真实凭证待配置验证。

生产资源：原创美术和音效通过对象存储/CDN 发布；当前工程不配置线上账号、不上传素材。

## 十、接口和核心实体

接口细节见 docs/API.md。

- 初始化：场景、商品、余额、库存、当前会话和服务端时间。
- 虚拟兑换：商品与幂等请求编号。
- 开始体验：地点、商品与幂等请求编号；服务器生成时间及事件。
- 结束体验：按会话 ID 幂等结束。
- 每日补给：根据服务端自然日防重复领取。

当前数据库表：smoke_players、smoke_tokens、smoke_wallet_ledger、smoke_migrations。库存、兑换、会话、偏好与领取日期存于玩家聚合；目录与事件种子仍通过源码维护。规模增长后可拆分规范化表。

## 十一、迭代与验收

### 阶段 0：本次基础工程

完成大纲、前后端结构、三个页面、演示 API、事件规则、构建命令、领域及接口测试。数据库与美术分清已实现和待接入。

### 阶段 1：核心体验制作

完成两套原创场景、双手动作、打火机与烟灰缸素材，细化自然燃烧、烟灰、烟雾、音效和震动。完成不同屏幕比例与切后台恢复的真机验收。

验收重点：手与道具接触自然；长按和取消能恢复；场景切换不破坏当前会话；事件不增加操作步骤；中端设备目标 30 FPS，具体机型实测确认。

### 阶段 2：服务持久化与运营

微信登录、PostgreSQL、事务和幂等账本、素材资源管理、事件运营维护。兑换余额不能为负，并发请求不能重复扣款，时间与价格不能被客户端修改。

### 阶段 3：提审与上线准备

正式 AppID、主体/类目、备案、合法请求域名、隐私说明、资源授权与内容审核确认。审核是否通过尚未确定，不能从同类产品存在推导保证。

## 十二、边界与待办

- 不宣称「模拟一支等于少抽一支」、戒烟疗效或健康改善。
- 不复制参考产品的特色表达，不提供实物购买导流。品牌参考包装需确认使用权限后才可公开发布。
- 平台小程序/小游戏类目和内容要求需要在正式投入前确认。
- 本地演示余额可被修改；API 模式已使用服务端随机 Bearer 凭证，游客只供开发，生产要求微信登录。
- 后端数据已落盘，重启恢复经过测试；嵌入式数据库按单进程使用，生产备份与独立数据库部署待运维配置。
- 本轮交付程序化插画场景、双手和合成音效；真实微信授权、微信专用编译器、真机和独立 PostgreSQL 尚未实测。

## 参考资料

- 微信原生小程序文档：https://developers.weixin.qq.com/miniprogram/dev/framework/
- 微信官方 TypeScript 类型：https://github.com/wechat-miniprogram/api-typings
- NestJS 文档：https://docs.nestjs.com/
- PostgreSQL 文档：https://www.postgresql.org/docs/

