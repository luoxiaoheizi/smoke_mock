## 2026-09-21 虚拟余额、按包购买与广告奖励

- npm run check：37 项测试、类型检查、前后端构建、小程序产物和图片来源校验通过。
- 新增覆盖：品牌参考价扣款、20 支入库、旧存档保留、三档广告奖励、第四次拒绝、北京时间跨日、过期记录、账号隔离、并发幂等、未完整观看、SDK 加载失败/超时、退出清理、发奖失败后重试。
- UI 检查与 Premium strict audit / designmd lint 通过；84 组页面/宽度/状态排版检查通过。钱包长状态包含待领取和发奖错误文案。已人工查看钱包及商店截图。
- 按用户回复取消分享奖励。广告采用开发模拟与 SDK 回调桩测试，没有真实广告位、真实广告收益或微信真机验证；服务端尚无平台可信观看凭证，限制详见广告奖励接入.md。
- 本轮未提交、推送或部署。

## 2026-09-21 品牌参考商品接入

- 新增中华（硬）、钻石（荷花）、玉溪（软）、白沙（硬），四张本地包装原图约 212 KiB；记录来源 URL、SHA-256 与未确认商用授权状态。
- 商店、详情、收藏使用原生 image，完整显示包装；失败降级为名称占位。品牌参考优先陈列，原有存档与免费款保留。
- npm run check：通过类型检查、26 项测试、全量构建、小程序产物与图片来源哈希校验。新增四款兑换/幂等/选用/扣库存测试、品牌跨页与图片失败测试；API 断言四款目录完整。
- node scripts/verify-ui.mjs、Premium strict audit、designmd lint：通过。
- 浏览器转换预览：84 个页面/宽度/状态组合通过，所有展示的图片加载成功。已人工查看商店和中华详情截图。
- 验证范围为本地构建、API 集成、页面逻辑与浏览器排版；微信开发者工具及真机仍待验证。未提交或推送。

# UI 优化与功能复核 · 2026-09-21

本轮依据 Frontend Design Premium 优化七个页面的字体、配色、点击区域、按钮、加载/错误/空状态；保留现有 Canvas 美术和业务规则。

## 修复与回归覆盖

1. 商品详情仅在 onLoad 获取数据，从钱包领取补给后返回仍显示旧余额。改为 onShow 刷新，并防止重复刷新。
2. 历史/钱包在刷新时可以同时加载下一页，存在重复或覆盖风险。增加刷新、分页互斥；钱包领取与分页互斥。
3. 未点燃会话结束失败后无条件进入 burning。改为还原原阶段，并保留可见错误与重试操作。

以上三项均先复现失败测试，再修复至通过。另外补充地点选择/活动会话不重抽事件、设置保存失败恢复测试。

## 已执行检查

- npm run check：24 项测试全部通过；前后端与共享包 TypeScript、构建、7 页原生资源/事件绑定检查通过。
- node scripts/verify-ui.mjs：标签闭合、按钮语义、令牌映射、原生导航颜色、6 组关键正文对比度通过。
- Frontend Design Premium audit_project.py --mode strict：0 错误、0 警告，报告位于 artifacts/premium-audit.json。该审计器不解析 WXSS/WXML，原生模板检查由上述项目脚本补充。
- 官方 designmd lint DESIGN.md：0 错误、0 警告；没有新增项目依赖或修改 package-lock.json，CLI 仅使用项目缓存。
- check-preview.cjs：320/390/430px 下，7 页各 ready/loading/error/long 四状态，共 84 个组合通过横向溢出、按钮命名、键盘聚焦、文字令牌检查；包含窄屏场景面板和长情境文案。
- capture-preview.cjs：7 页截图无浏览器脚本错误。人工检查页面和窄屏面板，修正预览脚本误替换 --color-text 变量以及按钮占宽导致标题换行的问题。

预览入口：artifacts/preview/index.html；状态检查报告：artifacts/preview/layout-report.json；截图同目录。预览文件和测试数据库均被 Git 忽略。

## 本轮验证边界

浏览器预览只负责布局转换，不模拟微信运行时。微信开发者工具编译、真机授权/触控/原生 picker/读屏/性能仍待验证；Canvas 未新增系统减少动态适配。数据库自动测试仍使用隔离 PGlite，不代表已验证独立 PostgreSQL 部署。本轮未部署或推送。

---

# v0.2 验证记录

日期：2026-09-18。环境：Windows、Node.js 22.12.0、npm 10.9.0。

## 自动验证

npm run check 已通过：

- TypeScript：小程序、NestJS、共享领域包均通过。
- 19 项测试全部通过，0 失败。
- 前后端构建通过。
- 7 个页面的 JS/WXML/WXSS/JSON、Tab 路由、事件绑定、原生标签和音效资源检查通过。
- npm audit --omit=dev：0 个已知漏洞。

测试范围：

1. 北京时间、跨午夜、星期约束、权重、冷却、重复事件与全周兜底。
2. DTO 拒绝额外价格/事件字段、无效身份和非法分页参数。
3. 同一兑换编号并发重试只扣款一次；余额不足时不超买、不变负。
4. 玩家隔离、活动会话唯一、开局幂等、结束幂等。
5. 偏好更新、库存检查、每日补给去重与独立钱包账本一致性。
6. 进度修订号防乱序、剩余量防倒退、结束后不可修改。
7. 关闭后端与数据库后，用同一数据目录重新启动，验证令牌、余额、偏好、库存、会话与事件文案恢复。
8. 访问凭证过期及刷新，刷新保持原用户。
9. 前端真实 Page 逻辑：橱窗/详情兑换、选用、开局、点燃、长按、松手、抖灰、切后台恢复、结束、历史和补给。
10. 免费基础款与音效偏好保存。

HTTP 测试使用回环随机端口和隔离 PGlite 数据目录，结束关闭服务与数据库。未启动长期后台服务。

## 视觉验证

- 从实际 WXML/WXSS 生成浏览器视觉转换预览，共 7 页。
- 使用无头 Edge 截图，检查全部页面布局；无浏览器脚本错误。
- Canvas 渲染器是小程序与预览共用的同一份代码。
- 截图在 artifacts/preview，开发预览入口为 artifacts/preview/index.html。
- 预览只验证布局和 Canvas 图像，不模拟完整微信运行时；系统 Tab、导航和 switch 控件可能与真机不同。

## 尚未验证

- 微信开发者工具专用编译器、真实微信授权、真机性能与音效播放。
- 没有提供真实 AppID/AppSecret，微信登录分支未连接真实账号。
- 独立 PostgreSQL 连接池适配器已实现；本轮实际数据库测试采用落盘 PGlite，没有启动 Docker 或远程数据库。
- 没有上线、提审、发布或创建 Git 提交。

结论：主要页面和基本后端功能通过工程测试，可以开始微信开发者工具与真机联调；不能据此声称已经满足上线条件。

